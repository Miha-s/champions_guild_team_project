#ifndef GRAMMAR_HPP
#define GRAMMAR_HPP
#include "SyntaxRule.hpp"
#include <vector>

class Grammar
{
public:
    Grammar();

private:
    std::vector<SyntaxRule> grammar_rules;
    std::vector<
};

#endif // GRAMMAR_HPP
